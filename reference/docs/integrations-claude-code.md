# Claude Code Integration

Entire has first-class integration with Claude Code, automatically capturing sessions and checkpoints via Git hooks.

- **Session Detection**: Entire detects when Claude Code starts.
- **Nested Sessions**: Captures sub-agents spawned via the Task tool.
- **Rewind**: Supports `entire rewind` during active sessions.
- **Explain**: AI-powered explanations of commits.
