# Introduction

Entire is a new developer platform that hooks into your Git workflow to capture AI agent sessions on every push, unifying your code with its context and reasoning.

## Checkpoints
A new primitive that automatically captures agent context as first-class, versioned data in Git.
Capture includes transcripts, prompts, files touched, token usage, tool calls, and more.
