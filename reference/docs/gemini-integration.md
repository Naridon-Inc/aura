# Entire Gemini CLI Integration

- Detects when Gemini CLI is running.
- Tracks file changes and creates checkpoints on commit.
- Supports `entire rewind` and `entire resume`.
- Captures session boundaries and timestamps.
