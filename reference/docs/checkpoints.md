# Entire Checkpoints

- Created when you or the agent run `git commit`.
- Session data is attached to the commit via trailers.
- Metadata stored in `entire/checkpoints/v1`.
- Bidirectional linking allows finding context from any commit.
- Supports AI-generated summaries of sessions.
