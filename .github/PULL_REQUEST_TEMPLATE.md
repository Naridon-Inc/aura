## What this changes

<!-- The behaviour that is different after this lands, in a sentence or two.
     Why it should be different, if that is not obvious. -->

Closes #

## How it was tested

<!-- The commands you ran, and anything you checked by hand. CI runs:
       cargo build --release
       cargo test --test e2e -- --test-threads=4
       cargo test --bin aura -- --test-threads=4
-->

- [ ] `cargo test --bin aura` passes
- [ ] `cargo test --test e2e` passes
- [ ] A bug fix has a test that fails without the fix
- [ ] `cargo fmt` has been run
